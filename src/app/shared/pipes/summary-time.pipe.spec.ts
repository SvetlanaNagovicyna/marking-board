import { SummaryTimePipe } from './summary-time.pipe';

describe('SummaryTimePipe', () => {
  it('create an instance', () => {
    const pipe = new SummaryTimePipe();
    expect(pipe).toBeTruthy();
  });
});
