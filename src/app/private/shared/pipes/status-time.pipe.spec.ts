import { StatusTimePipe } from './status-time.pipe';

describe('StatusTimePipe', () => {
  it('create an instance', () => {
    const pipe = new StatusTimePipe();
    expect(pipe).toBeTruthy();
  });
});
